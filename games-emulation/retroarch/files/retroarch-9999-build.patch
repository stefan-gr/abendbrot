diff --git a/qb/qb.params.sh b/qb/qb.params.sh
index e280400c07..70067688e6 100644
--- a/qb/qb.params.sh
+++ b/qb/qb.params.sh
@@ -62,7 +62,7 @@ opt_exists() # $opt is returned if exists in OPTS
 	err="$2"
 	eval "set -- $OPTS"
 	for OPT do [ "$opt" = "$OPT" ] && return; done
-	die 1 "Unknown option $err"
+	echo "Unknown option $err"
 }
 
 parse_input() # Parse stuff :V
@@ -106,7 +106,7 @@ parse_input() # Parse stuff :V
 			-h|--help) print_help; exit 0;;
 			--) break ;;
 			'') : ;;
-			*) die 1 "Unknown option $1";;
+			*) echo "Unknown option $1";;
 		esac
 		shift
 	done
